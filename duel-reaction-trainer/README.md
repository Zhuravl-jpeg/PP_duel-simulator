# 🎯 Дуэльный тренажёр реакции

Многопользовательское веб-приложение для тренировки скорости реакции с общими раундами между игроками.

## 📋 Описание проекта

Игроки соревнуются в реакции на визуальный сигнал. Сигнал появляется синхронно у всех участников, и каждый должен нажать клавишу как можно быстрее. Побеждает игрок с наименьшим временем реакции в раунде и наибольшим количеством набранных баллов.

### Ключевые особенности

- **Многопользовательский режим**: 2–5 игроков в одном раунде
- **Серверная фиксация времени**: все измерения проводятся на сервере для защиты от манипуляций
- **Система штрафов**: фальстарт (нажатие до сигнала) ведёт к потере балла и задержке в следующем раунде
- **Серии раундов**: минимум 5 раундов в матче, победитель определяется по общему количеству баллов
- **История дуэлей**: сохранение и просмотр результатов всех матчей
- **Глобальная таблица лидеров**: рейтинг всех игроков

## 🛠 Технологический стек

| Компонент | Технология |
|-----------|-----------|
| Фреймворк | **Next.js 15** (App Router) |
| Язык | **TypeScript** |
| База данных | **PostgreSQL** |
| ORM | **Drizzle ORM** |
| API | **tRPC** (type-safe API) |
| Аутентификация | **Better-auth** |
| Стили | **Tailwind CSS** |
| Валидация | **Zod** |
| Тестирование | **Jest + React Testing Library** |

## 📁 Структура проекта

```
duel-reaction-trainer/
├── src/
│   ├── app/                      # Next.js App Router
│   │   ├── api/
│   │   │   ├── auth/             # Better-auth маршрут
│   │   │   └── trpc/             # tRPC маршрут
│   │   ├── layout.tsx            # Корневой layout
│   │   └── page.tsx              # Главная страница
│   ├── server/
│   │   ├── api/
│   │   │   ├── root.ts           # Корневой роутер tRPC
│   │   │   ├── routers/          # Роутеры (раунды, матчи и т.д.)
│   │   │   └── trpc.ts           # Настройка tRPC
│   │   ├── auth/
│   │   │   └── auth.ts           # Better-auth конфигурация
│   │   └── db/
│   │       ├── index.ts          # Подключение к БД
│   │       ├── schema.ts         # Схема Drizzle
│   │       └── migrations/       # Миграции БД
│   ├── lib/
│   │   ├── trpc.ts               # tRPC React-клиент
│   │   └── providers.tsx         # Providers (tRPC, Query)
│   └── components/               # React-компоненты
├── src/__tests__/                # Тесты
├── .env.example                  # Шаблон переменных окружения
├── drizzle.config.ts             # Конфигурация Drizzle
├── next.config.ts                # Конфигурация Next.js
├── tailwind.config.ts            # Конфигурация Tailwind
├── tsconfig.json                 # TypeScript
└── package.json
```

## 🗄 Схема базы данных

### Таблицы

| Таблица | Описание |
|---------|----------|
| `users` | Пользователи (аутентификация, профиль) |
| `matches` | Матчи (серии раундов) |
| `rounds` | Отдельные раунды внутри матча |
| `match_participants` | Участники матча с баллами и штрафами |
| `round_results` | Результаты каждого игрока в раунде |

### Связи

```
users (1) ──── (N) match_participants
matches (1) ──── (N) rounds
matches (1) ──── (N) match_participants
rounds (1) ──── (N) round_results
```

## 🚀 Быстрый старт

### Требования

- **Node.js** 18+
- **PostgreSQL** 14+
- **npm** / **yarn** / **pnpm**

### Установка

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd duel-reaction-trainer

# 2. Установите зависимости
npm install

# 3. Настройте переменные окружения
cp .env.example .env
# Отредактируйте .env, указав параметры подключения к БД

# 4. Настройте базу данных
npm run db:push      # Создать таблицы в БД
# или
npm run db:migrate   # Применить миграции

# 5. Запустите сервер разработки
npm run dev
```

### Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск сервера разработки (localhost:3000) |
| `npm run build` | Сборка продакшн-версии |
| `npm run start` | Запуск продакшн-сервера |
| `npm run lint` | Линтинг кода |
| `npm run db:generate` | Генерация миграций |
| `npm run db:migrate` | Применение миграций |
| `npm run db:push` | Push схемы в БД (без миграций) |
| `npm run db:studio` | Открытие Drizzle Studio (GUI для БД) |
| `npm test` | Запуск тестов |
| `npm run test:watch` | Запуск тестов в режиме watch |

## 🔒 Безопасность

### Защита от манипуляций временем

- **Сервер является единственным авторитетным источником времени**. Клиент отправляет только факт нажатия, а не измеренное время.
- `Date.now()` на клиенте **не используется** для расчёта времени реакции.
- Сервер фиксирует момент отправки сигнала и момент получения ответа от клиента.

### Аутентификация

- Используется **Better-auth** с cookie-based сессиями.
- Защищённые tRPC-процедуры требуют аутентификации.
- Валидация всех входных данных через **Zod**.

## 📊 Логика раунда

### Ход раунда

1. Игроки присоединяются к матчу (2–5 человек)
2. Сервер начинает раунд и фиксирует момент отправки сигнала
3. Сигнал появляется синхронно у всех участников
4. Игроки нажимают клавишу как можно быстрее
5. Сервер фиксирует время реакции каждого
6. Побеждает игрок с наименьшим временем реакции → +1 балл
7. При фальстарте (нажатие до сигнала): −1 балл + задержка в следующем раунде

### Защита от фальстарта

- Если игрок нажимает клавишу **до** появления сигнала → фальстарт
- Штраф: −1 балл + задержка в следующем раунде
- Результат фиксируется сервером на основе `signalTime`

## 📝 План разработки

| Фаза | Описание | Статус |
|------|----------|--------|
| 1 | Инфраструктура: Next.js, Drizzle, Better-auth, tRPC, структура | ✅ Выполнено |
| 2 | Серверная логика ядра: модели, механизмы, логика | ⏳ В процессе |
| 3 | Защита от манипуляций: синхронизация, серверное время | ⏳ В процессе |
| 4 | tRPC-маршруты: создание раундов, отправка результатов | ⏳ В процессе |
| 5 | Клиентский UI: лобби, раунд, результаты, история | ⏳ В процессе |
| 6 | Тестирование: юнит- и интеграционные тесты | ⏳ В процессе |
| 7 | Боты: эмуляция игроков с настраиваемой реакцией | ⏳ В процессе |
| 8 | Таблица лидеров: глобальный рейтинг | ⏳ В процессе |

## 📄 Лицензия

MIT
